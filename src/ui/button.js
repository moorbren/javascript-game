import {MenuButtonTS} from './style.js'

/**
 * 
 * 
 * @param {Object} textStyle Text styles all contained within style.js
 * @param {String} sprite String naming the sprite to be used. These should be loaded before scenes are created.
 * @param {Number} x 
 * @param {Number} y 
 */
export function createButton(textStyle, sprite, x, y){
    
}